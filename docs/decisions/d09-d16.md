# 決策 D9–D16

> **TL;DR**
> - 可測性、crate 劃分、team 模型、main 前進政策、claude driver。
> - 記住：D16 的「CLAUDE.md 來源說明」是實測結果（0/3 → 3/3），不是猜測。
> - 下一步：回 [../DECISIONS.md](../DECISIONS.md) 找其他決策。

每條格式：決定 · 理由 · 否決的方案 · 證據。

「規劃 §x」指設計階段計畫 [REWRITE-PLAN.md](../research/REWRITE-PLAN.md)；其他證據檔在 [../research/](../research/README.md)。

## D9

- 決定：每個模組都能獨立驗證與測試（規劃 §5.1）：依賴單向、外部邊界有 trait + 假實作、契約測試、輸入用 producer 產生、每個模組可單獨跑起來。
- 理由：避免假綠測試；讓人與 agent 都能驗證單一模組。
- 否決的方案：未另外記錄（使用者在設計討論中決定）。
- 證據：v1 #1483（matcher 比對的格式 production 從不送出，測試全綠但是死碼）、#1493 規則；v1 有 48 個 invariant／guard 測試在補架構該保證的事（[history.md §5](../research/history.md)）。

## D10

- 決定：crate 劃分四條準則——(1) 編譯器強制的限制；(2) 獨立程序、有自己的穩定性要求；(3) 對外使用；(4) 多 crate 共用的測試基礎設施。其餘為模組 + trait。新增 `agend-client`、`agend-testkit`（dev-only）、`xtask`。
- 理由：切 crate 本身不是簡化（v1 架構 RFC §4 明列禁止的論證：「把模組邊界調整（拆 crate、拆 binary）本身當作簡化的證明」，見 [v1-architecture-rfc.md](../research/v1-architecture-rfc.md)）；只在四種情況切：(1) 需要編譯器強制的限制，(2) 獨立程序有自己的穩定性要求，(3) 有外部使用者，(4) 多個 crate 共用的測試基礎設施。
- 否決的方案：更少 crate、沒有 client／testkit（CLI 與 shim 需要輕量的同步 client；每個 crate 各抄一份假實作會漂移——v1 #1483 測試對著 production 從不送出的格式全綠）；每個 adapter 一個 crate（碎片化，卻沒有要強制的邊界）。
- 證據：規劃 §5、architecture 頁 crate 圖。

## D11

- 決定：protocol 放 core（外部 GUI 用產生的 JSON schema）；agend-client 同步 I/O；driver／forge／store 為 daemon 內模組 + trait；holder 與 daemon 同一 binary、每個 instance 一個 holder；流水線狀態機（6 種關卡 + task 關係與操作）放 core 為純函式。
- 理由：protocol 型別放 core，所有 client 共用一份定義，非 Rust 的 GUI 用產生的 JSON schema；client 用同步 I/O 讓 CLI／shim 啟動快（實測 binary 啟動 p50 4.1 ms、socket 來回 0.014 ms）；driver／forge／store 不符合任何一條切 crate 準則，而且 trait 已讓它們能單獨測；holder 用同一個 binary 方便發布，每個 instance 一個是為了故障隔離（runtime spike：持有 PTY 的程序被硬殺，它持有的所有 PTY 一起死）；流水線狀態機放 core，每個轉換都能不經 I/O 測試。
- 否決的方案：獨立的 protocol crate；以 tokio 為基礎的 client；每個 adapter 一個 crate；所有 agent 共用一個 holder。
- 證據：規劃 §4.7、§5.1 表格。

## D12

- 決定：每個 instance 同時只屬於一個 team。內建不可刪除的預設 team `general`；沒指定 team 的 instance 與 task 都歸它，不存在「沒有 team」的特例；`general` 也有共享目錄。
- 理由：一個 instance 只屬於一個 team，首頁每個 team 的區塊就不會有重複成員；內建 `general` 讓程式與 UI 都沒有「沒有 team」的特例。
- 否決的方案：一個 instance 屬於多個 team；team 可為空值。
- 證據：規劃 §2 D12。

## D13

- 決定：`teams/<team>/` 放 team 說明（注入給成員）、共享筆記、共享產出；成員各自的 workspace 不放進去。Telegram：一個「需要你」topic + 每個 team 一個 topic；個別 instance topic 可選、非預設。
- 理由：team 共享目錄讓成員共用指示、筆記與產出，又不混到個人 workspace；Telegram「需要你」topic 對應首頁的「需要你」區，各 team topic 放摘要；v1 的個別 instance topic 在 agent 變多後很難找。
- 否決的方案：預設每個 instance 一個 topic（改為可選）。
- 證據：規劃 §1、§2 D13。

## D14

- 決定：main 前進後自動 rebase 並重跑 checks；rebase 無衝突且 branch 自身 diff 的 `git patch-id` 不變則保留核准，否則退回 work。
- 理由：reviewer 核准的是變更的內容，不是某個 SHA；乾淨 rebase 後 `git patch-id` 不變，代表 diff 正是被審過的那份，重跑 checks 則證明合併後的結果有測過。它回答了規劃 §4.5 原本待定的兩個問題。
- 否決的方案：設計討論中提出三個選項，使用者選 B（本決定）。A：main 每前進一次就重新審查——流水線會卡住；C：不重跑就直接 merge——可能 merge 沒測過、沒審過的組合。（來源：設計討論，未另存於 research。）
- 證據：規劃 §4.5（待定條目）→ §2 D14。

## D15

- 決定：team 有 0 或 1 個 repo。workflow 宣告需求（`code` 需要 repo；`research` 不需要）；無 repo 的 team 沒有 worktree 與 shim；`general` 無 repo。跨 repo 工作用跨 team 的 `depends_on`。
- 理由：v1 有真實的無 repo 工作（`general` 助理、nulls-research team）；workflow 以 `requires = ["repo"]` 宣告，規則是資料，不是特例。
- 否決的方案：每個 team 都必須有 repo；一個 team 多個 repo（跨 repo 工作改用跨 team 的 `depends_on`）。
- 證據：v1 真實 task 有跨 repo 依賴只能寫在描述裡（[workflow-validation.md §5](../research/workflow-validation.md)，`t-20260719142708627000-80319-34`）。

## D16

- 決定：claude driver 採互動式 TUI + hooks。狀態看 hooks；排隊用 Stop hook `decision: block`（先取出佇列再回傳，`stop_hook_active` 防迴圈）；閒置時經 channel 送；中斷 = `Esc` 後立即經 channel 送（Esc 不觸發 Stop，不做等待）。專案 CLAUDE.md 必須說明 agend channel 訊息來自使用者自己的團隊；訊息內可另加 from／task／request 標頭。
- 理由：channel 在忙碌時送到的訊息可能不被處理；Stop hook 可可靠地注入一次後續 turn。
- 否決的方案：忙碌時直接經 channel 送（C1：訊息被擱置）；Esc 後等 Stop 再送（F3：Esc 後不會有 Stop）；`-p` stream-json 的 `control_request interrupt`（只在 headless 模式驗證過，互動模式未驗證）。
- 證據：[spike-claude.md](../research/spike-claude.md) C1–C5；[spike-claude-f.md](../research/spike-claude-f.md)：無說明 0/3、CLAUDE.md 說明 3/3、訊息標頭單獨 3/3、兩者並用 3/3、`claude-opus-5` 2/2、Stop hook 排隊 3/3 無迴圈。

## 下一步

```bash
cat docs/decisions/d17-d25.md
```
