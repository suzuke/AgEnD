[package]
name = "xtask"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
serde_json = "1"

[dev-dependencies]
agend-core = { path = "../crates/agend-core" }
# The frozen client protocol 1.0 types in tests/protocol_compat.rs.
serde = { version = "1", features = ["derive"] }
toml = "1.1.6"
