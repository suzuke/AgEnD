//! `agend doctor`: git version (merge-tree needs >= 2.38), gh login (github
//! forge only), each backend installed / version in tested range / logged in,
//! service status, disk, Telegram (token reachable, empty allowlist is an
//! error). Every finding comes with a fix command; `--json` supported.
//!
//! Must NOT: change anything.
